# Skills

这里存放可复用 Codex Skill。

当前 Skill：

- `adspower-browser/`：AdsPower 浏览器管理与页面会话相关能力。
- `shopee-hover-data/`：Shopee 图表 hover/sourceData 数据提取。
- `shopee-official-excel-export/`：Shopee 官方 Excel 导出。

使用规则：

- 先读对应目录下的 `SKILL.md`。
- 如果有 `references/`，按 `SKILL.md` 的说明读取相关参考文档。
- 如果有 `scripts/`，优先复用脚本，不要在聊天里重新手写大段逻辑。
- 真实导出、账号凭证、cookies、token、下载文件不要放进 Skill 目录提交。

