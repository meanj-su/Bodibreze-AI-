# 协作规范（公开版）

正式项目协作规则以仓库根目录 `AGENTS.md` 为准。

## 基本规则

- 新项目必须包含项目级 `AGENTS.md` 和 `README.md`。
- AI 开始任务前先说明已读取文件、复用知识、重新验证项和人工确认边界。
- 可公开、可复用、跨项目稳定的规则写入 `docs\framework` 或 `docs\public`。
- 本地路径、凭证路径、原始文件位置、未整理口径，保留在本地公共资料或数据资源库，不提交 GitHub。
- 项目临时结论不能直接升级为全局规则，必须经过验证和人工确认。
